export class Team {
    id: number;
    name: string;
    overall: number;

    constructor(id, name, overall) {
        this.id = id;
        this.name = name;
        this.overall = overall;
    }
}